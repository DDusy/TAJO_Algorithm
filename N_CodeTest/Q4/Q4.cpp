#include "Q4.h"

void Solution_Q4()
{
	vector<string> map;
	int map_len;

	cin >> map_len;
	string str;
	for (int i = 0; i < map_len; ++i)
	{
		cin >> str;
		map.push_back(str);
	}

#pragma region CODE
	// 여기에 코드를 입력하세요
#pragma endregion
}